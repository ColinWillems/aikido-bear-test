name: gamification
description: A modular library for managing achievements and rewards.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.6 <4.0.0"
  flutter: ">=3.22.1"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
