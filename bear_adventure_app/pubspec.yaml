name: bear_adventure_app
version: 5.0.2+52
description: The BEAR Adventure app.
publish_to: none
environment:
  sdk: ">=3.0.6 <4.0.0"

dependency_overrides:
  image: 4.5.3
  archive: 4.0.2
  intl: 0.20.2
  csslib: 1.0.0
# image: ^4.1.0
# mailer: ^6.0.1
# macos_ui:
#  git:
#    url: https://github.com/Mayb3Nots/macos_ui.git
#    ref: dev

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  bear_necessities:
    path: ../packages/bear_necessities
  flutter_app_utils:
    path: ../packages/flutter_app_utils
  firebase_app_utils:
    path: ../packages/firebase_app_utils
  cupertino_icons: ^1.0.9
  vector_math: ^2.1.4
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  firebase_crashlytics: ^4.3.4
  cloud_functions: ^5.2.0
  app_links: ^6.3.3
  play_install_referrer: ^0.5.0
  firebase_analytics: ^11.4.4
  firebase_storage: ^12.4.4
  firebase_performance: ^0.10.1
  cloud_firestore: ^5.6.5
  get: 5.0.0-release-candidate-9.3.2
  responsive_framework: ^1.5.1
  avoid_keyboard: ^0.3.0
  animated_toggle_switch: ^0.8.4
  circular_reveal_animation: ^2.0.1
  intl: ^0.20.2
  introduction_screen: ^4.0.0
  #  git: https://github.com/Pyozer/introduction_screen.git
  flutter_native_splash: ^2.4.8
  flutter_launcher_icons: ^0.14.3
  tinycolor2: ^3.0.1
  equatable: ^2.0.8
  photo_view: ^0.15.0
  flip_card: ^0.7.0
  decorated_text:
    git: https://github.com/StrangeTechDev/decorated_text.git
  gif_view: ^1.0.3
  # flutter_svg: ^2.0.0+1
  lottie: ^3.3.0
  dotlottie_loader: ^0.0.4
  flutter_animate: ^4.5.2
  rate_limiter: ^1.0.0
  nanoid: ^1.0.0
  adaptive_dialog: ^2.8.0
  path_provider: ^2.1.5
  path: ^1.9.1
  shared_preferences: ^2.5.2
  permission_handler: ^11.4.0
  pdf: ^3.11.3
  printing: ^5.14.3
  image_editor_plus: ^1.0.6
  # open_filex: ^4.6.0
  open_filex: ^4.6.0
  # NOTE: previously pinned to https://github.com/Sameetdmr/open_filex
  # (a fork that has since been deleted, breaking CI). Reverted to the
  # upstream pub.dev package — verify regression tests on file open
  # flows after this change.
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  json_annotation: ^4.9.0
  freezed_annotation: ^3.0.0
  device_info_plus: ^11.3.3
  package_info_plus: ^8.3.0
  mailer: ^6.4.1

  #mobile_scanner: ^3.0.0-beta.4
  google_mlkit_barcode_scanning: ^0.14.1
  camerawesome:
    # git: https://github.com/StrangeTechDev/CamerAwesome.git
    git: https://github.com/Apparence-io/CamerAwesome.git
  # camerawesome: ^2.0.1
  # camerawesome:
  #   git: https://github.com/iBog/CamerAwesome.git
  # code_scan:
  #  git:
  #    url: https://github.com/DrafaKiller/CodeScanner-flutter.git
  # camera: ^0.11.1
  url_launcher: ^6.3.1
  #flutter_cache_manager_firebase: ^2.0.1
  profanity_filter: ^2.0.0
  screenshot: ^3.0.0
  timelines_plus: ^2.0.0
  gif: ^2.3.0
  non_uniform_border: ^1.0.0
  # catcher:
  #  git: https://github.com/sumitsharansatsangi/catcher.git
  scroll_snap_list: ^0.9.1
  flutter_html: ^3.0.0
  bordered_text: ^3.0.0
  image: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.15
  flutter_gen_runner: ^5.14.1
  package_rename: ^1.9.0
  json_serializable: ^6.9.4
  freezed: ^3.0.0

get_cli:
  separator: .

package_rename_config:
  android:
    app_name: BEAR Adventure
    # package_name: # (String) The package name of the android app
  ios:
    app_name: BEAR Adventure
    # bundle_name: # (String) The bundle name of the ios app
    # package_name: # (String) The product bundle identifier of the ios app

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: ../packages/bear_necessities/assets/images/global/app_icon.png
  min_sdk_android: 21
  adaptive_icon_background: ../packages/bear_necessities/assets/images/global/app_icon.png
  adaptive_icon_foreground: ../packages/bear_necessities/assets/images/global/app_icon.png

flutter_native_splash:
  # This package generates native code to customize Flutter's default white native splash screen
  # with background color and splash image.
  # Customize the parameters below, and run the following command in the terminal:
  # flutter pub run flutter_native_splash:create
  # To restore Flutter's default white splash screen, run the following command in the terminal:
  # flutter pub run flutter_native_splash:remove

  # color or background_image is the only required parameter.  Use color to set the background
  # of your splash screen to a solid color.  Use background_image to set the background of your
  # splash screen to a png image.  This is useful for gradients. The image will be stretch to the
  # size of the app. Only one parameter can be used, color and background_image cannot both be set.
  color: "#73BE3C"
  # background_image: ../packages/bear_necessities/assets/images/splash/splash_screen.gif

  # Optional parameters are listed below.  To enable a parameter, uncomment the line by removing
  # the leading # character.

  # The image parameter allows you to specify an image used in the splash screen.  It must be a
  # png file and should be sized for 4x pixel density.
  #image: assets/splash.png

  # The branding property allows you to specify an image used as branding in the splash screen.
  # It must be a png file. It is supported for Android, iOS and the Web.  For Android 12,
  # see the Android 12 section below.
  # branding: ../packages/bear_necessities/assets/images/global/app_icon.png

  # To position the branding image at the bottom of the screen you can use bottom, bottomRight,
  # and bottomLeft. The default values is bottom if not specified or specified something else.
  # branding_mode: bottom

  # The color_dark, background_image_dark, image_dark, branding_dark are parameters that set the background
  # and image when the device is in dark mode. If they are not specified, the app will use the
  # parameters from above. If the image_dark parameter is specified, color_dark or
  # background_image_dark must be specified.  color_dark and background_image_dark cannot both be
  # set.
  #color_dark: "#042a49"
  #background_image_dark: "assets/dark-background.png"
  #image_dark: assets/splash-invert.png
  #branding_dark: assets/dart_dark.png

  # Android 12 handles the splash screen differently than previous versions.  Please visit
  # https://developer.android.com/guide/topics/ui/splash-screen
  # Following are Android 12 specific parameter.
  android_12:
    # The image parameter sets the splash screen icon image.  If this parameter is not specified,
    # the app's launcher icon will be used instead.
    # Please note that the splash screen will be clipped to a circle on the center of the screen.
    # App icon with an icon background: This should be 960×960 pixels, and fit within a circle
    # 640 pixels in diameter.
    # App icon without an icon background: This should be 1152×1152 pixels, and fit within a circle
    # 768 pixels in diameter.
    #image: assets/android12splash.png

    # Splash screen background color.
    color: "#73BE3C"

    # App icon background color.
    #icon_background_color: "#111111"

    # The branding property allows you to specify an image used as branding in the splash screen.
    #branding: assets/dart.png

    # The image_dark, color_dark, icon_background_color_dark, and branding_dark set values that
    # apply when the device is in dark mode. If they are not specified, the app will use the
    # parameters from above.
    #image_dark: assets/android12splash-invert.png
    #color_dark: "#042a49"
    #icon_background_color_dark: "#eeeeee"

  # The android, ios and web parameters can be used to disable generating a splash screen on a given
  # platform.
  #android: false
  #ios: false
  #web: false

  # Platform specific images can be specified with the following parameters, which will override
  # the respective parameter.  You may specify all, selected, or none of these parameters:
  #color_android: "#42a5f5"
  #color_dark_android: "#042a49"
  #color_ios: "#42a5f5"
  #color_dark_ios: "#042a49"
  #color_web: "#42a5f5"
  #color_dark_web: "#042a49"
  #image_android: assets/splash-android.png
  #image_dark_android: assets/splash-invert-android.png
  #image_ios: assets/splash-ios.png
  #image_dark_ios: assets/splash-invert-ios.png
  #image_web: assets/splash-web.png
  #image_dark_web: assets/splash-invert-web.png
  #background_image_android: "assets/background-android.png"
  #background_image_dark_android: "assets/dark-background-android.png"
  #background_image_ios: "assets/background-ios.png"
  #background_image_dark_ios: "assets/dark-background-ios.png"
  #background_image_web: "assets/background-web.png"
  #background_image_dark_web: "assets/dark-background-web.png"
  #branding_android: assets/brand-android.png
  #branding_dark_android: assets/dart_dark-android.png
  #branding_ios: assets/brand-ios.png
  #branding_dark_ios: assets/dart_dark-ios.png

  # The position of the splash image can be set with android_gravity, ios_content_mode, and
  # web_image_mode parameters.  All default to center.
  #
  # android_gravity can be one of the following Android Gravity (see
  # https://developer.android.com/reference/android/view/Gravity): bottom, center,
  # center_horizontal, center_vertical, clip_horizontal, clip_vertical, end, fill, fill_horizontal,
  # fill_vertical, left, right, start, or top.
  android_gravity: fill_vertical
  #
  # ios_content_mode can be one of the following iOS UIView.ContentMode (see
  # https://developer.apple.com/documentation/uikit/uiview/contentmode): scaleToFill,
  # scaleAspectFit, scaleAspectFill, center, top, bottom, left, right, topLeft, topRight,
  # bottomLeft, or bottomRight.
  ios_content_mode: scaleAspectFill
  #
  # web_image_mode can be one of the following modes: center, contain, stretch, and cover.
  web_image_mode: cover

  # The screen orientation can be set in Android with the android_screen_orientation parameter.
  # Valid parameters can be found here:
  # https://developer.android.com/guide/topics/manifest/activity-element#screen
  android_screen_orientation: sensorPortrait

  # To hide the notification bar, use the fullscreen parameter.  Has no effect in web since web
  # has no notification bar.  Defaults to false.
  # NOTE: Unlike Android, iOS will not automatically show the notification bar when the app loads.
  #       To show the notification bar, add the following code to your Flutter app:
  #       WidgetsFlutterBinding.ensureInitialized();
  #       SystemChrome.setEnabledSystemUIOverlays([SystemUiOverlay.bottom, SystemUiOverlay.top]);
  # fullscreen: true

flutter:
  uses-material-design: true
  fonts:
    - family: Ursa
      fonts:
        - asset: assets/fonts/Ursa/Ursa-Major-900.otf
          weight: 900
        - asset: assets/fonts/Ursa/Ursa-Minor-700.otf
          weight: 700
